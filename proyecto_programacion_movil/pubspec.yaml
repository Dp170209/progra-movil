name: proyecto_programacion_movil
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8
  image_picker: ^1.1.2
  # Actualizamos a la última versión de Face Detection
  google_mlkit_face_detection: ^0.13.1    # :contentReference[oaicite:0]{index=0}
  firebase_storage: ^12.4.6
  provider: ^6.1.1
  dialogflow_flutter: ^1.0.0
  http: ^0.13.6
  googleapis_auth: ^1.4.1
  fl_chart: ^1.0.0
  path_provider: ^2.0.11
  speech_to_text: ^7.0.0
  flutter_tts: ^3.6.3
  lottie: ^2.2.0
  camera: ^0.9.8+1
  tflite_flutter: ^0.9.0
  tflite_flutter_helper: ^0.2.1

dependency_overrides:
  # Esta versión incluye el namespace en build.gradle
  google_mlkit_commons: ^0.11.0          # :contentReference[oaicite:1]{index=1}

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/clave_dialogflow.json
    - assets/confetti.json  